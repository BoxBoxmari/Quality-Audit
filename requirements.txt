# Core dependencies (required for main flow: OOXML/python-docx -> validate -> Excel)
pandas>=2.0.0
numpy>=2.0.0
openpyxl>=3.1.0
python-docx>=1.1.0

lxml>=5.1.0  # High-performance XML parsing for docx/openpyxl

# Render-first table extraction (DOCX -> PDF -> Image -> OCR -> Grid)
# Required:
Pillow>=10.0.0
opencv-python>=4.9.0
pytesseract>=0.3.10
PyMuPDF>=1.24.0  # fitz - PDF rendering
pdf2image>=1.17.0  # Alternative PDF renderer (requires poppler)
# Optional (for multi-language OCR):
# easyocr>=1.7.0  # GPU recommended

# Optional: Table Transformer (advanced structure recognition for merged cells)
# Install torch first with your preferred backend:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then install transformers:
#   pip install transformers timm
# or uncomment below for CPU-only PyTorch:
# torch>=2.2.0
# transformers>=4.38.0

# System binaries (for render-first only; NOT installed by pip - install on each machine/OS):
# - Poppler: PDF tools (pdftoppm). apt: poppler-utils; Windows: choco install poppler; macOS: brew install poppler
# - Tesseract: OCR. apt: tesseract-ocr; Windows/macOS: installer from project site
# - LibreOffice: DOCX->PDF (headless soffice). Install per OS.

# Utilities
python-dotenv>=1.0.0

# Development and Quality Assurance
pytest>=8.0.0
ruff>=0.3.0     # Modern linter/formatter (replaces flake8, isort)
black>=24.0.0   # Matches ruff-compatible formatting
mypy>=1.10.0
python-Levenshtein>=0.25.0  # For CER evaluation
