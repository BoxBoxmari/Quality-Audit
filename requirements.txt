pandas>=1.5.0
numpy>=1.23.0

openpyxl>=3.0.0
python-docx>=0.8.11
customtkinter>=5.2.2

Pillow>=10.0.0
opencv-python>=4.9.0
pytesseract>=0.3.10
PyMuPDF>=1.24.0
pdf2image>=1.17.0

# NOTE:
# - legacy/main.py is the primary canonical runtime (does not require ML deps).
# - TableTransformerRecognizer (render-first advanced structure) is optional and
#   requires torch + transformers when enabled/available.
torch
transformers
